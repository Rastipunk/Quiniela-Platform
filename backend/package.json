{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "prisma generate && tsc",
    "start": "prisma migrate deploy && node dist/server.js",
    "seed:admin": "ts-node-dev --transpile-only src/scripts/seedAdmin.ts",
    "seed:test-accounts": "ts-node-dev --transpile-only src/scripts/seedTestAccounts.ts",
    "seed:wc2026-sandbox": "ts-node-dev --transpile-only src/scripts/seedWc2026Sandbox.ts",
    "seed:test-scenario": "ts-node-dev --transpile-only src/scripts/seedTestScenario.ts",
    "seed:auto-advance-test": "ts-node-dev --transpile-only src/scripts/seedAutoAdvanceTest.ts",
    "seed:test-review": "ts-node-dev --transpile-only src/scripts/seedTestReview.ts",
    "migrate:add-usernames": "ts-node-dev --transpile-only src/scripts/migrateAddUsername.ts",
    "test:tournament-advancement": "ts-node-dev --transpile-only src/scripts/testTournamentAdvancement.ts",
    "clean:instance": "ts-node-dev --transpile-only src/scripts/cleanInstance.ts",
    "check:instance-state": "ts-node-dev --transpile-only src/scripts/checkInstanceState.ts",
    "test:all-fixes": "ts-node-dev --transpile-only src/scripts/testAllFixes.ts",
    "script:force-completed": "ts-node-dev --transpile-only src/scripts/forcePoolCompleted.ts",
    "script:prepare-completion": "ts-node-dev --transpile-only src/scripts/preparePoolForCompletion.ts",
    "script:list-pools": "ts-node-dev --transpile-only src/scripts/listPools.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "google-auth-library": "^10.5.0",
    "jsonwebtoken": "^9.0.3",
    "prisma": "^6.19.1",
    "resend": "^6.6.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^25.0.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
